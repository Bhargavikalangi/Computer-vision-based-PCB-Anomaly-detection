import DashboardView from '@/components/dashboard/DashboardView';

export const metadata = {
  title: 'Dashboard — PCB Vision',
};

export default function DashboardPage() {
  return <DashboardView />;
}
